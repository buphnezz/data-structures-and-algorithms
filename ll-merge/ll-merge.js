'use strict';

let firstLinkedList = (1, 2, 3, 4);
let secondLinkedList = (a, b, c, d);
let thirdLinkedList = ('');
let firstListCounter = 0;
let secondListCounter = 0;

let mergedList = function(firstLinkedList, secondLinkedList) {
firstLinkedList.append(Head).thirdLinkedList;

while (secondListCounter <= firstListCounter - 1 && firstListCounter > secondListCounter) {
  secondLinkedList.append(Head).thirdLinkedList;
  secondListCounter +=
  firstListCounter +=

mergedList();
thirdLinkedList(n);
};
};
