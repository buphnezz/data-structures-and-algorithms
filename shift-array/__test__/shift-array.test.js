'use strict';

const shiftArray = require('../lib/shift-array');
describe('shift-array.test.js', () => {
  describe('shift-array.hi', () => {
    test('Array should not be empty', () => {
      expect(greet.hi(null)).toEqual('Array should not be empty!');
    });
})});
