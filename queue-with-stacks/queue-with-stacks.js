'use strict';

const stackOne = {a, b, c, d};
const stackTwo = {e, f, g, h};


class Queue {
  constructor() {
    this.stackOne.pop(),
    this.stackTwo.pop(),
  }
  while (stackOne !== undefined && stackTwo !== undefined) {
    Queue.constructor.enque()
  }
  }
  constructor();
};
