'use strict';

const targetValue = 0;

let queue = {};

traverseKAry(KaryTree) {
  if (!root.node) {
    return null;
  }
  current.node = root.node;

  while (let i = 0; current.node.child.length < length; i++) {
    if ( current.node.value === targetValue) {
      queue.enqueue.current.node.value
    }
    return queue;
  }
};
